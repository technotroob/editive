# Copy this file to .env.local and fill in your API keys.
# These keys are server-side only and are never exposed to the browser.
REMOVE_BG_API_KEY=
CLIPDROP_API_KEY=
OCR_SPACE_API_KEY=
REPLICATE_API_TOKEN=